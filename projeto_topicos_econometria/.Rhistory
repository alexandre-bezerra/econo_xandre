tema_abnt
ggsave("graficos/Grafico7_Eficiencia.png", plot = grafico_A_s,
width = 16, height = 14, units = "cm", dpi = 600)
# 7. Eficiência de Matching
dados_grafico_as <- dados_calibrados %>%
select(Tempo, A_eff) %>%
pivot_longer(
cols = c(A_eff),
names_to = "Variavel",
values_to = "Valor"
) %>%
mutate(
Painel = case_when(
Variavel == "A_eff" ~ "Eficiência de Matching (A)"
),
Painel = factor(Painel, levels = c(
"Eficiência de Matching (A)"
))
)
grafico_A_s <- ggplot(dados_grafico_as, aes(x = Tempo, y = Valor)) +
geom_line(linewidth = 1, color = "black") +
geom_point(size = 2, color = "black") +
facet_wrap(~ Painel, ncol = 1, scales = "free_y") +
geom_vline(xintercept = 2020.0, linetype = "dashed", color = "gray40", linewidth = 0.8) +
geom_text(
data = data.frame(Painel = factor(c("Eficiência de Matching (A)"))),
aes(x = 2020.1, y = Inf, label = "Choque COVID-19"),
vjust = 1.5, hjust = 0, size = 3.5, color = "gray40", family = "serif"
) +
labs(
title = "Gráfico 7 – Evolução da Eficiência de Matching (A)",
x = "Ano",
y = "Índice",
caption = "Fonte: Elaborado pelo autor (2026) com dados da PNADc e CAGED."
) +
tema_abnt
ggsave("graficos/Grafico7_Eficiencia.png", plot = grafico_A_s,
width = 16, height = 14, units = "cm", dpi = 600)
grafico_A_s <- ggplot(dados_grafico_as, aes(x = Tempo, y = Valor)) +
geom_line(linewidth = 1, color = "black") +
geom_point(size = 2, color = "black") +
facet_wrap(~ Painel, ncol = 1, scales = "free_y") +
geom_vline(xintercept = 2020.0, linetype = "dashed", color = "gray40", linewidth = 0.8) +
geom_text(
data = data.frame(Painel = factor(c(""))),
aes(x = 2020.1, y = Inf, label = "Choque COVID-19"),
vjust = 1.5, hjust = 0, size = 3.5, color = "gray40", family = "serif"
) +
labs(
title = "Gráfico 7 – Evolução da Eficiência de Matching (A)",
x = "Ano",
y = "Índice",
caption = "Fonte: Elaborado pelo autor (2026) com dados da PNADc e CAGED."
) +
tema_abnt
ggsave("graficos/Grafico7_Eficiencia.png", plot = grafico_A_s,
width = 16, height = 14, units = "cm", dpi = 600)
grafico_A_s <- ggplot(dados_grafico_as, aes(x = Tempo, y = Valor)) +
geom_line(linewidth = 1, color = "black") +
geom_point(size = 2, color = "black") +
facet_wrap(~ Painel, ncol = 1, scales = "free_y") +
geom_vline(xintercept = 2020.0, linetype = "dashed", color = "gray40", linewidth = 0.8) +
geom_text(
data = data.frame(Painel = factor(c("Eficiência de Matching (A)"))),
aes(x = 2020.1, y = Inf, label = "Choque COVID-19"),
vjust = 1.5, hjust = 0, size = 3.5, color = "gray40", family = "serif"
) +
labs(
title = "Gráfico 7 – Evolução da Eficiência de Matching (A)",
x = "Ano",
y = "Índice",
caption = "Fonte: Elaborado pelo autor (2026) com dados da PNADc e CAGED."
) +
tema_abnt
ggsave("graficos/Grafico7_Eficiencia.png", plot = grafico_A_s,
width = 16, height = 14, units = "cm", dpi = 600)
setwd("C:/Users/alexa/ProjetosGIT/econo_xandre/projeto_topicos_econometria")
library(basedosdados)
library(tidyverse)
# Coleta de Dados do CAGED
set_billing_id("didipdf")
query_caged <- "
SELECT
ano,
mes,
id_municipio,
SUM(saldo_movimentacao) AS saldo_empregos
FROM `basedosdados.br_me_caged.microdados_movimentacao`
WHERE ano >= 2020
AND cnae_2_secao IN ('G', 'I') -- G: Comércio; I: Alojamento e Alimentação
GROUP BY ano, mes, id_municipio
ORDER BY id_municipio, ano, mes
"
dados_caged_mensal <- read_sql(query_caged)
saveRDS(dados_caged_mensal, "dados/dados_caged_mensal.RDS")
View(dados_caged_mensal)
library(readr)
tarifa_zero <- read_delim("dados/tarifa_zero.csv",
delim = ";", escape_double = FALSE, trim_ws = TRUE)
View(tarifa_zero)
library(dplyr)
library(tidyr)
tarifa_zero <- tarifa_zero %>%
filter(ano_inicio >= 2020)
write.csv(tarifa_zero, file="tarifa_zero_filtrada.csv", row.names = FALSE, col.names = TRUE)
write.csv(tarifa_zero, file="tarifa_zero_filtrada.csv", row.names = FALSE, col.names = c("municipio"))
write.csv(tarifa_zero$municipio, file="tarifa_zero_filtrada.csv", row.names = FALSE)
library(tidyverse)
library(dplyr)
library(tidyr)
library(readr)
tarifa_zero <- read_csv("dados/tarifa_zero.csv")
View(tarifa_zero)
library(readr)
mes <- read_csv("dados/mes.csv")
View(mes)
df_tratamento_completo <- tarifa_zero %>%
left_join(
mes %>% select(municipio, mes_inicio),
by = c("municipio")
)
View(df_tratamento_completo)
write.csv(df_tratamento_completo, "dados/tarifa_zero.csv", row.names = FALSE)
# Importação de Tabela Tarifa Zero
df_tarifa_zero <- read_csv2("dados/tarifa_zero.csv")
# Importação de Tabela Tarifa Zero
df_tarifa_zero <- read_csv2("dados/tarifa_zero.csv")
View(df_tarifa_zero)
# Importação de Tabela Tarifa Zero
df_tarifa_zero <- read_csv("dados/tarifa_zero.csv")
dados_caged_mensal <- readRDS("dados/dados_caged_mensal.RDS")
View(dados_caged_mensal)
# CAGED
df_caged_final <- dados_caged_mensal %>%
filter(!is.na(id_municipio)) %>%
mutate(
codigo_ibge = as.numeric(id_municipio),
periodo = (ano * 100) + mes
) %>%
select(codigo_ibge, periodo, saldo_empregos)
View(df_caged_final)
# Tarifa Zero
df_tratamento <- df_tarifa_zero %>%
mutate(
codigo_ibge = as.numeric(codigo_ibge),
coorte_tratamento = (as.numeric(ano_inicio) * 100) + as.numeric(mes_inicio)
) %>%
select(codigo_ibge, coorte_tratamento)
# Merge
painel_did <- df_caged_final %>%
left_join(df_tratamento, by = "codigo_ibge") %>%
mutate(
coorte_tratamento = ifelse(is.na(coorte_tratamento), 0, coorte_tratamento)
)
View(painel_did)
View(df_tratamento)
# Verifica se deu tudo certo
glimpse(painel_did)
# Coleta de Dados de População
query_populacao <- "
SELECT
id_municipio,
populacao
FROM `basedosdados.br_ibge_populacao.municipio`
WHERE ano = 2019
"
df_populacao <- read_sql(query_populacao) %>%
mutate(codigo_ibge = as.numeric(id_municipio)) %>%
select(codigo_ibge, populacao)
# Carregamento das bibliotecas necessárias
library(basedosdados)
df_populacao <- read_sql(query_populacao) %>%
mutate(codigo_ibge = as.numeric(id_municipio)) %>%
select(codigo_ibge, populacao)
# Coleta de Dados do CAGED
set_billing_id("didipdf")
df_populacao <- read_sql(query_populacao) %>%
mutate(codigo_ibge = as.numeric(id_municipio)) %>%
select(codigo_ibge, populacao)
View(df_populacao)
# Filtro Populacional
painel_did_restrito <- painel_did %>%
left_join(df_populacao, by = "codigo_ibge") %>%
filter(!is.na(populacao)) %>%
filter(populacao >= 30000 & populacao <= 400000)
View(painel_did_restrito)
glimpse(painel_did_restrito)
library(did)
install.packages("did")
# ==========================================
# 1. CARREGAR O PACOTE DID
# ==========================================
# install.packages("did") # Rode isso uma vez se ainda não tiver instalado
library(did)
# ==========================================
# 2. ESTIMAÇÃO DE CALLAWAY & SANT'ANNA (2021)
# ==========================================
# Aqui calculamos o Efeito Médio do Tratamento para cada grupo (g) e tempo (t)
out_att <- att_gt(
yname = "saldo_empregos",       # Sua variável dependente
tname = "periodo",              # O tempo numérico (ex: 202301)
idname = "codigo_ibge",         # O ID do município
gname = "coorte_tratamento",    # O tempo de adoção (0 para controle)
data = painel_did_restrito,     # Nossa base filtrada e limpa!
control_group = "nevertreated", # O controle são as cidades que nunca adotaram
bstrap = TRUE,                  # Usa Bootstrap para erros-padrão robustos
cband = TRUE                    # Calcula bandas de confiança simultâneas
)
# CAGED
df_caged_final <- dados_caged_mensal %>%
filter(!is.na(id_municipio)) %>%
mutate(
codigo_ibge = as.numeric(as.character(id_municipio)),
periodo = as.numeric(as.character((ano * 100) + mes)),
saldo_empregos = as.numeric(as.character(saldo_empregos))
) %>%
select(codigo_ibge, periodo, saldo_empregos)
# balanceamento
df_caged_final <- df_caged_final %>%
tidyr::complete(
codigo_ibge,
periodo,
fill = list(saldo_empregos = 0)
)
# Tarifa Zero
df_tratamento <- df_tarifa_zero %>%
mutate(
codigo_ibge = as.numeric(codigo_ibge),
coorte_tratamento = (as.numeric(ano_inicio) * 100) + as.numeric(mes_inicio)
) %>%
select(codigo_ibge, coorte_tratamento)
# Merge
painel_did <- df_caged_final %>%
left_join(df_tratamento, by = "codigo_ibge") %>%
left_join(df_populacao, by = "codigo_ibge") %>%
mutate(
coorte_tratamento = ifelse(is.na(coorte_tratamento), 0, coorte_tratamento),
populacao = as.numeric(as.character(populacao))
) %>%
filter(!is.na(populacao)) %>%
filter(populacao >= 30000 & populacao <= 400000)
glimpse(painel_did_restrito)
glimpse(painel_did)
# ==========================================
# 2. ESTIMAÇÃO DE CALLAWAY & SANT'ANNA (2021)
# ==========================================
# Aqui calculamos o Efeito Médio do Tratamento para cada grupo (g) e tempo (t)
out_att <- att_gt(
yname = "saldo_empregos",       # Sua variável dependente
tname = "periodo",              # O tempo numérico (ex: 202301)
idname = "codigo_ibge",         # O ID do município
gname = "coorte_tratamento",    # O tempo de adoção (0 para controle)
data = painel_did_restrito,     # Nossa base filtrada e limpa!
control_group = "nevertreated", # O controle são as cidades que nunca adotaram
bstrap = TRUE,                  # Usa Bootstrap para erros-padrão robustos
cband = TRUE                    # Calcula bandas de confiança simultâneas
)
View(df_populacao)
library(basedosdados)
library(tidyverse)
library(dplyr)
library(tidyr)
# Importação de Tabela Tarifa Zero
df_tarifa_zero <- read_csv("dados/tarifa_zero.csv")
View(df_tarifa_zero)
# Importação de Tabela Tarifa Zero
df_tarifa_zero <- read_csv("dados/tarifa_zero.csv")
# Coleta de Dados do CAGED
set_billing_id("didipdf")
query_caged <- "
SELECT
ano,
mes,
id_municipio,
SUM(saldo_movimentacao) AS saldo_empregos
FROM `basedosdados.br_me_caged.microdados_movimentacao`
WHERE ano >= 2020
AND cnae_2_secao IN ('G', 'I') -- G: Comércio; I: Alojamento e Alimentação
GROUP BY ano, mes, id_municipio
ORDER BY id_municipio, ano, mes
"
dados_caged_mensal <- read_sql(query_caged)
# Coleta de Dados de População
query_populacao <- "
SELECT
id_municipio,
populacao
FROM `basedosdados.br_ibge_populacao.municipio`
WHERE ano = 2019
"
df_populacao <- read_sql(query_populacao) %>%
mutate(
codigo_ibge = as.numeric(as.character(id_municipio)),
populacao = as.numeric(as.character(populacao))
) %>%
select(codigo_ibge, populacao)
View(df_populacao)
View(dados_caged_mensal)
# CAGED
df_caged_final <- dados_caged_mensal %>%
filter(!is.na(id_municipio)) %>%
mutate(
codigo_ibge = as.numeric(as.character(id_municipio)),
periodo = as.numeric(as.character((ano * 100) + mes)),
saldo_empregos = as.numeric(as.character(saldo_empregos))
) %>%
select(codigo_ibge, periodo, saldo_empregos)
View(df_caged_final)
# balanceamento
df_caged_final <- df_caged_final %>%
tidyr::complete(
codigo_ibge,
periodo,
fill = list(saldo_empregos = 0)
)
# Tarifa Zero
df_tratamento <- df_tarifa_zero %>%
mutate(
codigo_ibge = as.numeric(as.character(codigo_ibge)),
coorte_tratamento = (as.numeric(as.character(ano_inicio)) * 100) + as.numeric(as.character(mes_inicio))
) %>%
select(codigo_ibge, coorte_tratamento)
View(df_tratamento)
# Merge
painel_did <- df_caged_final %>%
left_join(df_tratamento, by = "codigo_ibge") %>%
left_join(df_populacao, by = "codigo_ibge") %>%
mutate(
coorte_tratamento = ifelse(is.na(coorte_tratamento), 0, coorte_tratamento),
populacao = as.numeric(as.character(populacao))
) %>%
filter(!is.na(populacao)) %>%
filter(populacao >= 30000 & populacao <= 400000)
glimpse(painel_did)
# ==========================================
# 1. CARREGAR O PACOTE DID
# ==========================================
# install.packages("did") # Rode isso uma vez se ainda não tiver instalado
library(did)
# ==========================================
# 2. ESTIMAÇÃO DE CALLAWAY & SANT'ANNA (2021)
# ==========================================
# Aqui calculamos o Efeito Médio do Tratamento para cada grupo (g) e tempo (t)
out_att <- att_gt(
yname = "saldo_empregos",       # Sua variável dependente
tname = "periodo",              # O tempo numérico (ex: 202301)
idname = "codigo_ibge",         # O ID do município
gname = "coorte_tratamento",    # O tempo de adoção (0 para controle)
data = painel_did,     # Nossa base filtrada e limpa!
control_group = "nevertreated", # O controle são as cidades que nunca adotaram
bstrap = TRUE,                  # Usa Bootstrap para erros-padrão robustos
cband = TRUE                    # Calcula bandas de confiança simultâneas
)
# ==========================================
# 2. ESTIMAÇÃO DE CALLAWAY & SANT'ANNA (2021)
# ==========================================
# Aqui calculamos o Efeito Médio do Tratamento para cada grupo (g) e tempo (t)
out_att <- att_gt(
yname = "saldo_empregos",       # Sua variável dependente
tname = "periodo",              # O tempo numérico (ex: 202301)
idname = "codigo_ibge",         # O ID do município
gname = "coorte_tratamento",    # O tempo de adoção (0 para controle)
data = painel_did,     # Nossa base filtrada e limpa!
control_group = "nevertreated", # O controle são as cidades que nunca adotaram
bstrap = TRUE,                  # Usa Bootstrap para erros-padrão robustos
cband = FALSE,
panel = FALSE,
allow_unbalanced_panel = TRUE
)
# ==========================================
# 3. ESTUDO DE EVENTOS (AGREGAÇÃO DINÂMICA)
# ==========================================
# O att_gt calcula dezenas de efeitos. O aggte() resume tudo em um
# "tempo até o tratamento" (onde o mês de adoção vira o momento zero).
out_es <- aggte(out_att, type = "dynamic")
# Resumo estatístico no console
summary(out_es)
# Plotar o gráfico com intervalos pontuais
ggdid(out_es) +
ggplot2::theme_minimal() +
ggplot2::labs(
title = "Estudo de Eventos: Tarifa Zero e Emprego",
x = "Meses relativos ao Tratamento (0 = Mês de Adoção)",
y = "ATT (Saldo de Empregos)"
)
out_att <- att_gt(
yname = "saldo_empregos",       # Sua variável dependente
tname = "periodo",              # O tempo numérico (ex: 202301)
idname = "codigo_ibge",         # O ID do município
gname = "coorte_tratamento",    # O tempo de adoção (0 para controle)
data = painel_did,     # Nossa base filtrada e limpa!
control_group = "nevertreated", # O controle são as cidades que nunca adotaram
bstrap = TRUE,                  # Usa Bootstrap para erros-padrão robustos
anticipation = 0,
cband = FALSE,
panel = FALSE,
allow_unbalanced_panel = TRUE
)
# ==========================================
# 3. ESTUDO DE EVENTOS (AGREGAÇÃO DINÂMICA)
# ==========================================
# O att_gt calcula dezenas de efeitos. O aggte() resume tudo em um
# "tempo até o tratamento" (onde o mês de adoção vira o momento zero).
out_es <- aggte(out_att, type = "dynamic")
# Resumo estatístico no console
summary(out_es)
# Plotar o gráfico com intervalos pontuais
ggdid(out_es) +
ggplot2::theme_minimal() +
ggplot2::labs(
title = "Estudo de Eventos: Tarifa Zero e Emprego",
x = "Meses relativos ao Tratamento (0 = Mês de Adoção)",
y = "ATT (Saldo de Empregos)"
)
saveRDS(dados_caged_mensal, "dados/dados_caged_mensal.rds")
saveRDS(df_populacao, "dados/df_populacao.rds")
# Correção do Painel
painel_corrigido <- painel_did_restrito %>%
mutate(
ano_atual = floor(periodo / 100),
mes_atual = periodo %% 100,
tempo_seq = (ano_atual - 2020) * 12 + mes_atual,
ano_trat = floor(coorte_tratamento / 100),
mes_trat = coorte_tratamento %% 100,
coorte_seq = ifelse(coorte_tratamento == 0, 0, (ano_trat - 2020) * 12 + mes_trat),
saldo_norm = (saldo_empregos / populacao) * 1000
) %>%
filter(!is.na(saldo_norm) & !is.na(tempo_seq))
# Correção do Painel
painel_corrigido <- painel_did %>%
mutate(
ano_atual = floor(periodo / 100),
mes_atual = periodo %% 100,
tempo_seq = (ano_atual - 2020) * 12 + mes_atual,
ano_trat = floor(coorte_tratamento / 100),
mes_trat = coorte_tratamento %% 100,
coorte_seq = ifelse(coorte_tratamento == 0, 0, (ano_trat - 2020) * 12 + mes_trat),
saldo_norm = (saldo_empregos / populacao) * 1000
) %>%
filter(!is.na(saldo_norm) & !is.na(tempo_seq))
out_att_corrigido <- att_gt(
yname = "saldo_norm",           # USAMOS A NOVA VARIÁVEL NORMALIZADA
tname = "tempo_seq",            # USAMOS O TEMPO SEQUENCIAL (1, 2, 3...)
idname = "codigo_ibge",
gname = "coorte_seq",           # USAMOS A COORTE SEQUENCIAL
data = painel_corrigido,
control_group = "nevertreated",
bstrap = TRUE,
cband = FALSE,                  # Mantemos FALSE para estabilidade
panel = FALSE,
allow_unbalanced_panel = TRUE
)
# ==========================================
# 4. ESTUDO DE EVENTOS (AGREGAÇÃO DINÂMICA)
# ==========================================
# Agregação I
#out_es <- aggte(out_att, type = "dynamic")
# Agregação II
out_es_corrigido <- aggte(out_att_corrigido, type = "dynamic")
# Resumo estatístico no console
summary(out_es_corrigido)
# Plotar o gráfico com intervalos pontuais
ggdid(out_es_corrigido) +
ggplot2::theme_minimal() +
ggplot2::labs(
title = "Efeito da Tarifa Zero no Emprego Local (Comércio e Serviços)",
subtitle = "Painel balanceado e corrigido para inflação demográfica",
x = "Meses antes e depois da adoção da Tarifa Zero (0 = Mês da Lei)",
y = "Saldo de Empregos (por 1.000 habitantes)"
)
View(painel_corrigido)
library(ggplot2)
grafico_final <- ggdid(out_es_corrigido)
grafico_final +
theme_minimal(base_size = 14) + # Aumenta ligeiramente a letra para o ecrã
coord_cartesian(xlim = c(-24, 24)) +
scale_x_continuous(breaks = seq(-24, 24, by = 6)) +
geom_vline(xintercept = 0, linetype = "dashed", color = "red", size = 0.8) +
labs(
title = "Impacto da Tarifa Zero no Emprego (Comércio e Serviços)",
subtitle = "Estudo de Eventos: Janela restrita de 24 meses em torno da implementação",
x = "Meses relativos à implementação (0 = Início da Tarifa Zero)",
y = "Saldo de Empregos (por 1.000 habitantes)",
caption = "Fonte: Elaboração própria com dados do Novo CAGED e IBGE."
) +
theme(
plot.title = element_text(face = "bold", hjust = 0.5),
plot.subtitle = element_text(hjust = 0.5, color = "grey30"),
panel.grid.minor = element_blank() # Remove as linhas de grelha secundárias
)
grafico_final +
theme_minimal(base_size = 14) + # Aumenta ligeiramente a letra para o ecrã
coord_cartesian(xlim = c(-24, 24)) +
scale_x_continuous(breaks = seq(-24, 24, by = 6)) +
geom_vline(xintercept = 0, linetype = "dashed", color = "red", size = 0.8) +
labs(
title = "Impacto da Tarifa Zero no Emprego (Comércio e Serviços)",
subtitle = "Estudo de Eventos: Janela restrita de 24 meses em torno da implementação",
x = "Meses relativos à implementação (0 = Início da Tarifa Zero)",
y = "Saldo de Empregos (por 1.000 habitantes)",
caption = "Fonte: Elaboração própria com dados do Novo CAGED, IBGE e Santini (2024)."
) +
theme(
plot.title = element_text(face = "bold", hjust = 0.5),
plot.subtitle = element_text(hjust = 0.5, color = "grey30"),
panel.grid.minor = element_blank() # Remove as linhas de grelha secundárias
)
gc()
dir_temp <- tempdir()
arquivos_lixo <- list.files(dir_temp, full.names = TRUE,
pattern = "\\.zip$|\\.txt$|PNADC")
unlink(arquivos_lixo, recursive = TRUE, force = TRUE)
